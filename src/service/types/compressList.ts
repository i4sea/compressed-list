export type CompressItemsData = {
  id: number;
  title: string;
};

export type CompressList = {
  items: CompressItemsData[];
};

export type ReturnCompressList = {
  id: number;
  title: string;
};
